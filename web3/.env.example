# Web3 environment example (optional - only needed if you deploy or verify contracts)
# Copy to web3/.env when you need to deploy or verify contracts

# Private key (no 0x prefix). WARNING: keep this secret and never commit it.
PRIVATE_KEY=

# Optional: Etherscan (or verifier) API key for contract verification
ETHERSCAN_API_KEY=
