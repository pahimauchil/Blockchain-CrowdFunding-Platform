# Server environment example
# Copy this to server/.env and fill values before running the server

PORT=5000
NODE_ENV=development

# MongoDB connection (Atlas or local)
# Example (Atlas): mongodb+srv://<user>:<password>@cluster0.mongodb.net/crowdfunding?retryWrites=true&w=majority
# Example (local): mongodb://localhost:27017/crowdfunding
MONGODB_URI=mongodb://localhost:27017/crowdfunding

# JWT secret used to sign tokens (use a random string in production)
JWT_SECRET=change_this_to_a_random_secret

# Optional: Groq API key for AI analysis. Leave empty to use rule-based fallback.
GROQ_API_KEY=
